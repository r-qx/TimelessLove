# TimelessLove 开发提示词

## 项目背景

TimelessLove（永恒之爱）是一款专为情侣打造的任务管理与情感游戏化小程序。通过将日常心愿转化为趣味任务，让情侣在完成挑战中增进默契，积累爱心值升级关系，兑换奖励，记录美好瞬间。

---

## 核心概念速记

### 关键词
- **心愿任务**: 情感驱动的共同目标（非chores式任务）
- **爱心值**: 完成任务获得，用于升级CP等级
- **爱情币**: 虚拟货币，可兑换实物/虚拟奖励/心愿券
- **CP等级**: 共30级，从"新手情侣"到"TimelessLove"
- **双向奔赴**: 强调双方共同参与和确认
- **趣味惩罚**: 温情化的小代价，促进互动
- **瞬间**: 任务完成后的照片/视频记录
- **默契系统**: 通过测试了解彼此
- **时光轴**: 情侣专属的私密记录空间

### 设计哲学
1. **情感第一**: 所有功能都服务于促进情感连接
2. **仪式感**: 重要时刻的特殊展示和庆祝
3. **游戏化**: 用玩的方式让相处更有趣
4. **温情化**: 即使是惩罚也要甜蜜
5. **私密性**: 双方专属空间，绝对隐私

---

## 开发时的思考框架

### 在开发任何功能时，问自己：

1. **这个功能是否促进了双方互动？**
   - ✅ 好：需要双方确认/协作/互动
   - ❌ 避免：单方面操作，对方无感知

2. **这个功能是否有仪式感？**
   - ✅ 好：特殊动画、音效、视觉反馈
   - ❌ 避免：冷冰冰的操作完成

3. **这个功能是否情感化？**
   - ✅ 好：温暖的文案、鼓励性反馈、共情式提醒
   - ❌ 避免：机械化、工具化的语言

4. **这个功能是否尊重双方意愿？**
   - ✅ 好：需要双方同意，可拒绝
   - ❌ 避免：强制、单方面决定

5. **这个功能是否平衡了游戏性和现实？**
   - ✅ 好：有趣但不脱离实际
   - ❌ 避免：过于复杂或不切实际

---

## 功能开发优先级（MVP）

### P0 - 核心必须（先做）
1. ✅ 微信登录注册
2. ✅ 情侣配对绑定（邀请码）
3. ✅ 任务系统基础版
   - 创建任务（个人/双人）
   - 任务列表
   - 任务详情
   - 完成任务（上传证明）
   - 任务验收
4. ✅ 爱心值与等级系统
5. ✅ 基础聊天（文字/图片）
6. ✅ 个人中心

### P1 - 重要体验（其次）
7. ✅ 时光轴展示
8. ✅ 瞬间发布与展示
9. ✅ 任务完成动画
10. ✅ 官方任务库
11. ✅ 惩罚机制
12. ✅ 每日签到

### P2 - 增强功能（再后）
13. ✅ 爱情银行与心愿券
14. ✅ 成就徽章系统
15. ✅ 默契测试
16. ✅ 梦想储蓄罐
17. ✅ 回忆相册

### P3 - 高级功能（最后）
18. ⏳ 社区功能
19. ⏳ 会员系统
20. ⏳ 商城对接

---

## 数据模型速记

### 核心集合

#### users (用户表)
```typescript
{
  _id: ObjectId,
  openid: string,           // 微信openid
  nickname: string,         // 昵称
  avatar: string,           // 头像URL
  gender: 0|1|2,           // 0未知 1男 2女
  birthday: Date,           // 生日
  couple_id: ObjectId,      // 关联的情侣ID
  created_at: Date,
  updated_at: Date
}
```

#### couples (情侣关系表)
```typescript
{
  _id: ObjectId,
  user1_id: ObjectId,       // 用户1
  user2_id: ObjectId,       // 用户2
  anniversary: Date,        // 恋爱纪念日
  level: number,            // CP等级
  love_points: number,      // 爱心值
  love_coins: number,       // 爱情币
  intimacy: {               // 亲密度雷达图
    communication: number,  // 沟通
    cooperation: number,    // 协作
    romance: number,        // 浪漫
    understanding: number,  // 默契
    growth: number          // 成长
  },
  status: 'active'|'cooling'|'disbanded',
  created_at: Date,
  updated_at: Date
}
```

#### tasks (任务表)
```typescript
{
  _id: ObjectId,
  couple_id: ObjectId,
  creator_id: ObjectId,     // 创建者
  type: 'personal'|'cooperation'|'periodic'|'challenge',
  title: string,
  description: string,
  difficulty: 1-5,          // 难度星级
  reward_points: number,    // 爱心值奖励
  reward_coins: number,     // 爱情币奖励
  deadline: Date,           // 截止时间
  proof_type: string[],     // 证明类型: text/image/video/location/steps
  punishment: {             // 惩罚
    type: string,
    description: string
  },
  tags: string[],           // 标签
  status: 'pending'|'approved'|'in_progress'|'completed'|'failed',
  participants: ObjectId[], // 参与者
  proof: {                  // 完成证明
    user_id: ObjectId,
    content: string,
    media: string[],
    submitted_at: Date
  },
  verified_by: ObjectId,    // 验收人
  verified_at: Date,
  created_at: Date,
  updated_at: Date
}
```

#### moments (瞬间表)
```typescript
{
  _id: ObjectId,
  couple_id: ObjectId,
  user_id: ObjectId,        // 发布者
  task_id: ObjectId,        // 关联任务（可选）
  content: string,          // 文字内容
  media: [{                 // 媒体文件
    type: 'image'|'video',
    url: string
  }],
  mood: string,             // 心情标签
  location: {               // 位置
    name: string,
    latitude: number,
    longitude: number
  },
  likes: number,            // 点赞数
  created_at: Date
}
```

#### messages (消息表)
```typescript
{
  _id: ObjectId,
  couple_id: ObjectId,
  sender_id: ObjectId,
  type: 'text'|'image'|'voice'|'video'|'task'|'system',
  content: string,
  media_url: string,
  emotion: string,          // 情绪标签
  related_task_id: ObjectId, // 关联任务
  is_read: boolean,
  created_at: Date
}
```

#### achievements (成就表)
```typescript
{
  _id: ObjectId,
  couple_id: ObjectId,
  achievement_key: string,  // 成就标识
  name: string,
  description: string,
  icon: string,
  unlocked_at: Date
}
```

---

## API设计原则

### 命名规范
```
GET    /api/couples/:id              获取情侣信息
POST   /api/couples                  创建情侣关系
PATCH  /api/couples/:id              更新情侣信息

GET    /api/tasks                    获取任务列表
POST   /api/tasks                    创建任务
GET    /api/tasks/:id                获取任务详情
PATCH  /api/tasks/:id                更新任务
DELETE /api/tasks/:id                删除任务
POST   /api/tasks/:id/complete       完成任务
POST   /api/tasks/:id/verify         验收任务
```

### 响应格式统一
```typescript
// 成功
{
  code: 20000,
  message: '操作成功',
  data: { ... },
  timestamp: 1642492800000
}

// 失败
{
  code: 40400,
  message: '任务不存在',
  data: null,
  timestamp: 1642492800000
}
```

---

## UI/UX 关键点

### 色彩方案
```
主色: #FF6B9D (粉红)
辅助色: #FFA726 (橙色)
背景色: #FFF5F7 (浅粉)
文字: #333333 (深灰)
次要文字: #999999 (中灰)
成功: #4CAF50 (绿)
失败: #FF5252 (红)
```

### 关键动画
1. **任务完成**: 全屏撒花/烟花效果（2秒）
2. **等级提升**: 徽章放大+闪光效果（3秒）
3. **爱心增加**: 数字滚动+粒子效果
4. **签到**: 日历翻转动画
5. **消息发送**: 气泡弹出动画

### 文案风格
- **温暖**: "TA在等你一起完成呢~"
- **鼓励**: "太棒了！你们又解锁了一个甜蜜瞬间"
- **趣味**: "哎呀，任务失败了，来接受一个小惩罚吧😝"
- **共情**: "已经恋爱365天啦，感谢有你相伴"

---

## 关键业务逻辑

### 任务完成流程
```
1. 用户提交完成证明（图片/视频/文字）
   ↓
2. 系统通知对方验收
   ↓
3. 对方确认验收
   ↓
4. 发放奖励（爱心值+爱情币）
   ↓
5. 更新CP等级（如果达到阈值）
   ↓
6. 触发庆祝动画
   ↓
7. 自动生成瞬间建议
   ↓
8. 更新时光轴
   ↓
9. 检查成就解锁
```

### 等级计算
```typescript
// 等级与爱心值对照表
const LEVEL_THRESHOLDS = {
  1: 0,
  2: 100,
  3: 200,
  4: 350,
  5: 550,
  // ... 每级增长约150-200
  30: 50000
};

function calculateLevel(lovePoints: number): number {
  for (let level = 30; level >= 1; level--) {
    if (lovePoints >= LEVEL_THRESHOLDS[level]) {
      return level;
    }
  }
  return 1;
}
```

### 默契度计算
```typescript
// 根据不同维度的行为计算
function updateIntimacy(couple, action) {
  switch (action.type) {
    case 'chat_message':
      couple.intimacy.communication += 0.1;
      break;
    case 'task_complete':
      couple.intimacy.cooperation += 0.5;
      if (action.taskType === 'romantic') {
        couple.intimacy.romance += 0.3;
      }
      break;
    case 'quiz_correct':
      couple.intimacy.understanding += 1;
      break;
    // ...
  }
  // 各维度上限100
  normalizeIntimacy(couple.intimacy);
}
```

---

## 性能优化要点

### 小程序端
1. **分包策略**
   - 主包: 首页、任务、聊天（<2MB）
   - 分包1: 社区、商城（懒加载）
   - 分包2: 数据统计、设置

2. **图片优化**
   - 使用WebP格式
   - 启用懒加载
   - 缩略图+原图分离
   - CDN加速

3. **数据缓存**
   - 用户信息（本地存储）
   - 任务列表（缓存5分钟）
   - 聊天消息（本地数据库）

### 后端
1. **Redis缓存**
   - 用户session
   - 热门任务列表
   - 实时排行榜
   - 频繁查询的CP信息

2. **数据库索引**
   ```javascript
   // 常用查询建索引
   couples: [
     { user1_id: 1 },
     { user2_id: 1 },
     { level: -1 }
   ],
   tasks: [
     { couple_id: 1, status: 1 },
     { deadline: 1 }
   ],
   moments: [
     { couple_id: 1, created_at: -1 }
   ]
   ```

---

## 测试要点

### 核心场景测试
- [ ] 新用户注册并配对
- [ ] 创建任务（个人/双人）
- [ ] 完成任务全流程
- [ ] 等级提升
- [ ] 聊天收发消息
- [ ] 发布瞬间
- [ ] 默契测试
- [ ] 兑换奖励

### 边界情况
- [ ] 任务超时处理
- [ ] 网络异常恢复
- [ ] 并发操作冲突
- [ ] 分手后数据处理
- [ ] 重复绑定防护

---

## 上线检查清单

### 功能完整性
- [ ] 所有P0功能已实现
- [ ] 关键流程无阻塞bug
- [ ] 错误提示友好

### 性能
- [ ] 首屏加载<3秒
- [ ] 接口响应<500ms
- [ ] 无内存泄漏

### 安全
- [ ] 敏感信息加密
- [ ] 接口鉴权完整
- [ ] SQL注入防护
- [ ] XSS防护

### 合规
- [ ] 微信小程序规范
- [ ] 隐私政策
- [ ] 用户协议
- [ ] 内容审核机制

---

## 常见问题速查

### Q1: 一方完成任务，另一方未验收怎么办？
A: 48小时后自动通过验收，发放奖励。

### Q2: 情侣分手后数据怎么处理？
A: 保留个人数据（瞬间、消息可导出），删除共同数据（任务、等级等），7天冷静期可恢复。

### Q3: 如何防止刷爱心值？
A: 
- 每日任务完成数量上限
- 相同任务重复完成收益递减
- 异常行为检测与封禁机制

### Q4: 任务失败的惩罚谁来执行？
A: 任务创建时双方约定，失败方主动选择并完成惩罚（上传证明）。

### Q5: 如何保证用户留存？
A: 
- 每日签到奖励
- 周期性活动
- 推送恰当提醒（不过度）
- 持续更新任务库
- 纪念日特殊关怀

---

## 开发注意事项

### ⚠️ 绝对禁止
1. ❌ 在任何地方硬编码敏感信息
2. ❌ 没有错误处理的外部调用
3. ❌ 不安全的文件上传
4. ❌ 未经授权的数据访问
5. ❌ 过度的用户打扰（通知）

### ✅ 必须遵守
1. ✅ 所有用户输入必须验证
2. ✅ 关键操作必须记录日志
3. ✅ 数据库操作必须事务
4. ✅ 敏感操作需二次确认
5. ✅ 代码必须通过lint检查

---

## 快速开发参考

### 创建新模块的步骤
1. 在 `backend/src/modules/` 创建模块目录
2. 创建 `*.module.ts`, `*.controller.ts`, `*.service.ts`, `*.dto.ts`
3. 在 `app.module.ts` 中注册模块
4. 创建对应的 Schema/Interface
5. 编写 API 接口
6. 编写前端页面/组件
7. 联调测试

### 常用命令
```bash
# 后端开发
cd backend
npm run start:dev

# 小程序开发
cd miniprogram
npm run dev

# 数据库
docker-compose up -d mongodb redis
```

---

## 最后的话

**记住**: 这不只是一个任务管理工具，而是一个帮助情侣创造美好回忆的情感产品。

每一行代码，都在为某对情侣的幸福添砖加瓦。

用心开发，让爱永恒 💕

---

*本文档将随开发进展持续更新*
